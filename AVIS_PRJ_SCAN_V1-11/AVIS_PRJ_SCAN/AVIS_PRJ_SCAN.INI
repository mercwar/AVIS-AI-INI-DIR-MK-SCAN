===============================================================================
; AVIS PROJECT SCAN CONFIGURATION — TEMPLATE EDITION
; AVIS‑ACK‑BNF‑CYBORG DIRECTIVES ENABLED
; ============================================================================
; ARCH: x86_64 | GROUP ID: FFF | OWNER: <OWNER_NAME_PLACEHOLDER>
; ============================================================================
;  FIRE‑GEM KB EXTENSION — DIRECTORY GENERATION TEMPLATE
;  This file instructs LLMs how to RESOLVE UMSK and GENERATE the DIR section.
;  This is a TEMPLATE. LLMs MUST fill the [DIR] section when building.
; ============================================================================
;  AVIS‑ACK‑BNF‑CYBORG DIRECTIVES FOR LLMS:
;
;    <resolve-root> ::= "C:\" <PRJ_DRV_KEY> "\" <PRJ_DIR_KEY> <PRJ_ROOT_KEY>
;    <dir-entry>    ::= "DIR_" <NAME> "=" <absolute-path>
;
;    RULES:
;      - LLM MUST resolve UMSK keys before generating DIR entries.
;      - LLM MUST generate absolute Windows paths only.
;      - LLM MUST NOT insert spaces after "=".
;      - LLM MUST NOT invent new root paths.
;      - LLM MUST extend ONLY the resolved KB root.
;      - LLM MUST generate one DIR_* entry per directory required for BUILD.
;
;    AVIS‑ACK:
;      LLM:ACK[DIR.GEN] → ENABLE DIRECTORY GENERATION MODE
;      LLM:ACK[UMSK.RESOLVE] → ENABLE ROOT RESOLUTION MODE
;      LLM:ACK[ABSOLUTE.PATHS] → ENFORCE ABSOLUTE PATH OUTPUT
;
; ============================================================================
;  EXAMPLE ONLY — DO NOT USE THESE VALUES IN TEMPLATE:
;      PRJ_DRV_KEY=Apache24
;      PRJ_DIR_KEY=htdocs
;      PRJ_ROOT_KEY=\FIRE-GEM\KB
;      → RESOLVED ROOT = C:\Apache24\htdocs\FIRE-GEM\KB
;      → This key must be set at all times :: PRJ_APP_KEY=AVIS_PRJ_SCAN 
[UMSK]
;
; Full block example:
;
;PRJ_DRV_KEY=Apache24
;PRJ_DIR_KEY=htdocs
;PRJ_ROOT_KEY=\FIRE-GEM\KB
;PRJ_APP_KEY=AVIS_PRJ_SCAN

[AVIS_COORD]
; Reserved for future AVIS COORDINATOR metadata.
; Do NOT remove this header.


[DIR]
; ========================================================================
;  DIRECTORY STRUCT DEFINITIONS — GENERATED BY LLMS
; ========================================================================
;  AVIS‑ACK‑BNF‑CYBORG DIRECTIVES FOR LLMS:
;
;    LLM:ACK[UMSK.RESOLVE]
;       MUST compute:
;         C:\{PRJ_DRV_KEY}\{PRJ_DIR_KEY}{PRJ_ROOT_KEY}
;
;    LLM:ACK[DIR.GEN]
;       MUST generate entries in the form:
;         DIR_<NAME>=<absolute-path>
;
;    LLM:ACK[ABSOLUTE.PATHS]
;       MUST ensure:
;         - No symbolic placeholders
;         - No relative paths
;         - No leading spaces
;         - No spaces after "="
;
;    TEMPLATE EXAMPLE (LLM MUST FOLLOW THIS PATTERN):
;         DIR_BIN=C:\<DRV>\<DIR>\<ROOT>\BIN
;         DIR_INCLUDE=C:\<DRV>\<DIR>\<ROOT>\INCLUDE
;         DIR_LIB=C:\<DRV>\<DIR>\<ROOT>\lib
;         DIR_LOG=C:\<DRV>\<DIR>\<ROOT>\LOG
;         DIR_CFG=C:\<DRV>\<DIR>\<ROOT>\CFG
;         DIR_SRC=C:\<DRV>\<DIR>\<ROOT>\SRC
;         DIR_TMP=C:\<DRV>\<DIR>\<ROOT>\TMP
;
;    LLM MUST populate this section when BUILD is requested.
; ========================================================================
;  ROOT RESOLUTION KEYS — LLMs MUST RESOLVE THESE FIRST
; ========================================================================
;  AVIS‑ACK‑BNF‑CYBORG:
;     <root> ::= "C:\" <PRJ_DRV_KEY> "\" <PRJ_DIR_KEY> <PRJ_ROOT_KEY>
; ========================================================================
;  TEMPLATE PLACEHOLDERS — LLM MUST FILL THESE WHEN PROJECT IS CREATED
; ========================================================================
; ========================================================================
;  LLMs: Populate below this line.
; ========================================================================

; example test folder:
; uncomment the line below removing the ";" to test it 
;DIR_BIN=C:\CVBGOD


[cvbgod]